# MAVLink Simulator Setup Guide

## Option 1: PX4 SITL (Recommended)

### Prerequisites
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install git

# Clone PX4 repository
git clone https://github.com/PX4/PX4-Autopilot.git --recursive
cd PX4-Autopilot